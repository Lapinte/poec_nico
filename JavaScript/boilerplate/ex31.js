// JavaScript source code
let tableau = [9, 11, 55, 22];
/*tableau.sort(function (a, b) {
    return a - b;
})
console.log(tableau[0]);

var indexMax = tableau.length - 1
console.log(tableau[indexMax]);*/

console.log(Math.min(...tableau));
console.log(`Maximum: ${Math.max(...tableau)}`);